{
  "name": "@nfq/eslint-plugin",
  "version": "0.8.2",
  "description": "NFQ eslint rules",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Christoph Kruppe",
  "main": "lib/index.js",
  "scripts": {
    "deploy": "npx standard-version && git push --follow-tags origin master && npm publish",
    "test": "mocha tests --recursive"
  },
  "standard-version": {
    "scripts": {
      "prebump": "nfqBump"
    }
  },
  "dependencies": {
    "natural-compare": "^1.4.0",
    "requireindex": "~1.2.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "mocha": "^10.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC"
}
